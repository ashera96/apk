---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: applications.cp.wso2.com
spec:
  group: cp.wso2.com
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the applications API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application
            properties:
              attributes:
                additionalProperties:
                  type: string
                type: object
              authenticationOptions:
                items:
                  properties:
                    IPRange:
                      description: IPRange denotes the IPRange authentication option
                      properties:
                        IPRangeIdentifier:
                          type: string
                        IPs:
                          type: string
                        keyType:
                          type: string
                      required:
                      - IPRangeIdentifier
                      - IPs
                      - keyType
                      type: object
                    apiKey:
                      description: APIKey denotes the APIKey authentication option
                      properties:
                        key:
                          type: string
                        keyType:
                          type: string
                      required:
                      - key
                      - keyType
                      type: object
                    basicAuth:
                      description: BasicAuth denotes the Basic authentication option
                      properties:
                        basicAuthIdentifier:
                          type: string
                        keyType:
                          type: string
                      required:
                      - basicAuthIdentifier
                      - keyType
                      type: object
                    mTLS:
                      description: mTLS denotes the mTLS authentication option
                      properties:
                        cert:
                          type: string
                        issuer:
                          type: string
                        keyType:
                          type: string
                      required:
                      - cert
                      - issuer
                      - keyType
                      type: object
                    oauth2:
                      description: OAuth2 denotes the OAuth2 authentication option
                      properties:
                        consumerKey:
                          type: string
                        keyManager:
                          type: string
                        keyType:
                          type: string
                      required:
                      - consumerKey
                      - keyManager
                      - keyType
                      type: object
                    type:
                      description: Type denotes the application authentication type
                        Possible values are OAuth2, mTLS, BasicAuth, APIKey, IPRange
                      enum:
                      - OAuth2
                      - mTLS
                      - BasicAuth
                      - APIKey
                      - IPRange
                      type: string
                  required:
                  - type
                  type: object
                type: array
              name:
                type: string
              owner:
                type: string
            required:
            - authenticationOptions
            - name
            - owner
            type: object
          status:
            description: ApplicationStatus defines the observed state of Application
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
